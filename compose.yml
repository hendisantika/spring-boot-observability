services:
  mysql:
    image: mysql:9.2.0
    environment:
      MYSQL_USER: 'yu71'
      MYSQL_PASSWORD: '53cret'
      MYSQL_ROOT_PASSWORD: '53cret'
    ports:
      - "33081:3306"
    expose:
      - '3306'
    volumes:
      - ./mysql-data:/var/lib/mysql
      - ./docker/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - my-network
  tempo:
    image: grafana/tempo:2.2.2
    command: [ "-config.file=/etc/tempo.yaml" ]
    volumes:
      - ./docker/tempo/tempo.yml:/etc/tempo.yaml:ro
      - ./docker/tempo/tempo-data:/tmp/tempo
    ports:
      # Tempo's HTTP API — its own config sets http_listen_port: 3200.
      # (This was mapped 3110:3100, where nothing listens.)
      - "3200:3200"
      - "9411:9411" # zipkin ingest

  loki:
    image: grafana/loki:main
    command: [ "-config.file=/etc/loki/local-config.yaml" ]
    ports:
      - "3100:3100"

  prometheus:
    image: prom/prometheus:v2.46.0
    command:
      - --enable-feature=exemplar-storage
      - --config.file=/etc/prometheus/prometheus.yml
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    # prometheus.yml scrapes the services at host.docker.internal, which only
    # resolves on Docker Desktop. This makes it work on plain Linux (CI) too.
    extra_hosts:
      - "host.docker.internal:host-gateway"

  grafana:
    image: grafana/grafana:12.4.11
    volumes:
      - ./docker/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./docker/grafana/dashboards:/var/lib/grafana/dashboards:ro
    environment:
      # Admin credentials. Defaults are fine locally; set real ones in .env
      # (git-ignored) before exposing Grafana. See .env.example.
      - GF_SECURITY_ADMIN_USER=${GF_SECURITY_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD:-admin}
      # Grafana builds share links, alert links and OAuth redirects from this.
      # Override when proxied:
      #   GF_SERVER_ROOT_URL=https://grafana.jvm.my.id docker compose up -d
      - GF_SERVER_ROOT_URL=${GF_SERVER_ROOT_URL:-http://localhost:3000}
    ports:
      - "3000:3000"
volumes:
  mysql-data:

networks:
  my-network:
    driver: bridge

